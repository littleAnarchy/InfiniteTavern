# Infinite Tavern - Frontend

React frontend для AI-powered текстової RPG гри.

## 🚀 Швидкий старт

### 1. Встановити залежності
```bash
cd frontend
npm install
```

### 2. Запустити dev сервер
```bash
npm run dev
```

Фронтенд буде доступний на: **http://localhost:3000**

## 📋 Вимоги

- Node.js 18+
- Backend API запущений на порту 5000

## 🏗️ Технології

- **React 18** - UI фреймворк
- **TypeScript** - Type safety
- **Vite** - Швидкий dev сервер
- **CSS** - Стилізація (без додаткових бібліотек)

## 📁 Структура проекту

```
frontend/
├── src/
│   ├── components/
│   │   ├── CharacterCreation.tsx  # Форма створення персонажа
│   │   ├── GameView.tsx           # Головний ігровий екран
│   │   ├── PlayerStats.tsx        # Панель статистики
│   │   └── TurnHistory.tsx        # Історія ходів
│   ├── services/
│   │   └── gameService.ts         # API клієнт
│   ├── types/
│   │   └── game.ts                # TypeScript типи
│   ├── App.tsx                    # Головний компонент
│   ├── App.css                    # Стилі
│   └── main.tsx                   # Entry point
├── package.json
├── tsconfig.json
├── vite.config.ts
└── index.html
```

## 🎮 Як користуватись

### 1. Створення персонажа
- Введіть своє ім'я
- Створіть персонажа (ім'я, раса, клас)
- Натисніть "Begin Adventure"

### 2. Гра
- Введіть свою дію в поле внизу
- Натисніть "Act" або Enter
- AI згенерує відповідь та оновить стан гри
- Спостерігайте за своїм HP та статистикою

### 3. Події гри
- 💚 Зелений HP bar = здоров'я > 50%
- 🧡 Помаранчевий HP bar = 25-50%
- ❤️ Червоний HP bar = < 25%
- ⚡ Події відображаються під наративом

## 🔧 Доступні команди

```bash
# Розробка
npm run dev

# Збірка для продакшену
npm run build

# Перегляд продакшн збірки
npm run preview
```

## 🎨 Особливості UI

### Адаптивний дизайн
- Desktop: сайдбар зі статистикою + головний контент
- Mobile: стеки вертикально

### Темна тема
- Gradient background
- Purple accent color
- Smooth animations

### User Experience
- Auto-scroll до останнього ходу
- Loading states
- Error handling
- Confirmation на новій грі

## ⚙️ Конфігурація

### Proxy для API
Vite автоматично проксує `/api/*` запити на `http://localhost:5000`:

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:5000',
      changeOrigin: true
    }
  }
}
```

Якщо backend на іншому порту, змініть `target`.

## 🐛 Troubleshooting

### API не працює
```bash
# Перевірте чи запущений backend
curl http://localhost:5000/api/health
```

### CORS помилки
Backend має бути налаштований для прийняття запитів з `localhost:3000`.  
В `Program.cs` має бути:
```csharp
app.UseCors("AllowAll");
```

### Dependency помилки
```bash
# Видалити node_modules і переінсталювати
rm -rf node_modules package-lock.json
npm install
```

## 📦 Production Build

```bash
npm run build
```

Збірка буде в папці `dist/`. Можна розгорнути на:
- Netlify
- Vercel
- GitHub Pages
- Будь-який статичний хостинг

**Важливо:** Вкажіть правильний API URL для продакшену!

## 🔐 Environment Variables

Для продакшену створіть `.env.production`:
```
VITE_API_URL=https://your-api-domain.com
```

І оновіть `gameService.ts`:
```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || '/api/game';
```

## 📝 TODO (майбутні покращення)

- [ ] Persistent state (localStorage)
- [ ] Session restore
- [ ] Sound effects
- [ ] Quest tracker UI
- [ ] Character sheet export
- [ ] Dark/Light theme toggle
- [ ] Multiplayer chat

## 🤝 Використання разом з Backend

### Повний розгортання
1. Запустіть MongoDB: `docker-compose up -d`
2. Запустіть backend: `dotnet run --project src/InfiniteTavern.API`
3. Запустіть frontend: `cd frontend && npm run dev`
4. Відкрийте браузер: http://localhost:3000

---

**Приємної гри! 🎲⚔️🏰**
